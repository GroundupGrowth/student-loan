# Open Graph image — needed before launch

The metadata in `app/layout.tsx` references `/og-default.png`. That file does
not exist yet; replace this README with a real PNG before going live.

## Spec

- Dimensions: 1200 × 630
- Format: PNG (or WEBP if all target social platforms support it)
- Content: brand wordmark + tagline, on the navy brand background
- File size: < 300 KB ideally
